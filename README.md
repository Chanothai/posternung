# PosterNung (posternung)

A Flutter app for discovering and collecting rare, authenticated original movie posters.

[![CI](https://github.com/Chanothai/posternung/actions/workflows/ci.yml/badge.svg)](https://github.com/Chanothai/posternung/actions/workflows/ci.yml)

## Tech Stack

| Concern            | Choice                                            |
| ------------------ | -------------------------------------------------- |
| Framework           | Flutter (stable channel)                           |
| State management    | [Riverpod](https://pub.dev/packages/flutter_riverpod) |
| Architecture         | Clean Architecture, feature-first (see [CLAUDE.md](CLAUDE.md)) |
| Backend              | Firebase Auth today; more services as features need them |
| Fonts                | Google Fonts — Noto Serif Thai (headings), Sarabun (body/UI), Libre Baskerville (Latin brand title only) |
| Testing              | `flutter_test`, [`mocktail`](https://pub.dev/packages/mocktail) |
| CI/CD                | GitHub Actions (`.github/workflows/ci.yml`)         |

Architecture rules, folder conventions, and code patterns are documented in **[CLAUDE.md](CLAUDE.md)** — read that before adding a feature. Commit message and branching conventions are documented in **[CONTRIBUTING.md](CONTRIBUTING.md)**.

## Project Structure

```
posternung/
  lib/
    core/
      theme/                        # design tokens: AppColors, AppTextStyles (from Figma)
      error/                        # shared domain exception types (AuthException, ...)
      widgets/                      # cross-feature widgets (AppGradientBackground, ...)
    features/
      onboarding/
        presentation/
          providers/                # OnboardingController (Riverpod Notifier) — page index state
          screens/                  # OnboardingFirstScreen, OnboardingAuthenticateScreen, OnboardingLimitStockScreen
          widgets/                  # shared onboarding chrome: header, footer, progress dots, primary button
      auth/
        domain/                     # AuthUser entity, AuthRepository interface, sign-in/sign-up usecases
        data/                       # AuthRemoteDataSource + AuthRepositoryImpl (maps Firebase errors to AuthException)
        presentation/
          providers/                # DI graph + AuthViewModel (AsyncNotifier) for the login form
          screens/                  # LoginScreen (login/register, mode-toggled)
          auth_gate.dart            # gates a destination behind auth state
    home/                           # placeholder post-login screen (to be replaced by the real app shell)
    firebase_options.dart           # generated by `flutterfire configure` — safe to commit (public client config)
    main.dart                       # app bootstrap: Firebase.initializeApp, ProviderScope, root MaterialApp
  test/                             # mirrors lib/ — see CLAUDE.md testing conventions
  assets/
    images/                         # icons/art pulled from Figma
  android/ ios/ web/ macos/ linux/ windows/   # Flutter platform shells
  .github/workflows/ci.yml          # CI: format, analyze, test, build (android/ios/web)
```

`home/` is the only part of the app that still predates the Clean Architecture convention in CLAUDE.md (`onboarding` and `auth` are both fully feature-first) — see the "Features in development" note below.

## Features in Development

| Feature | Status | Notes |
| --- | --- | --- |
| **Onboarding** | 🟢 Feature-complete (3/3 screens) | `OnboardingFirstScreen` ("Own a Piece of Cinema History", Figma `6:2`), `OnboardingAuthenticateScreen` ("100% Authenticated Originals", Figma `7:2`), `OnboardingLimitStockScreen` ("Limited Stock — One of a Kind", Figma `7:90`, skip hidden on the last page). Shared page-index state via `onboardingControllerProvider`; Next navigates screen-to-screen, finishing into `AuthGate`. All copy is in Thai. Hero background on screen 1 is still a placeholder gradient — the Figma source frame's poster-collage fill hasn't been generated yet. |
| **Authentication** | 🟢 Feature-complete (email/password) | Fully migrated to Clean Architecture (`features/auth/{domain,data,presentation}`) — `AuthRepository` + `SignInWithEmailPassword`/`SignUpWithEmailPassword` usecases, `AuthRepositoryImpl` mapping `FirebaseAuthException` → domain `AuthException`, `AuthViewModel` (`AsyncNotifier`) driving `LoginScreen`'s loading/error state. `LoginScreen` matches Figma `7:130` (glassmorphism card, mode-toggled login/register). First feature in the app with real `mocktail` unit tests (usecases + repository) alongside a `ProviderScope`-overridden widget test. **Google/Apple sign-in buttons are UI-only** — rendered per design, tap shows a "coming soon" snackbar; wiring real OAuth needs new dependencies + Firebase console provider setup, out of scope until requested. Firebase project (`posternung`) is configured for Android/iOS/Web, but the **Email/Password sign-in provider still needs to be enabled in the Firebase Console** (Authentication → Sign-in method) before login/signup works at runtime end-to-end. |
| **Home / main app shell** | ⚪ Placeholder | `home/home_page.dart` is the stock Flutter counter demo, shown after a successful login. Needs to be replaced with the real post-onboarding app shell and migrated into a `features/home/` layout. |

Onboarding is the app's entry point (`main.dart` → `OnboardingFirstScreen`); each onboarding screen's Skip, or the last screen's "Get Started", pushes into `AuthGate`, which shows `LoginScreen` when signed out or the home shell when signed in.

## Getting Started

```bash
flutter pub get
flutter run
```

Firebase is already configured for this project (`lib/firebase_options.dart`, `android/app/google-services.json`, `ios/Runner/GoogleService-Info.plist`). If you need to reconfigure or add a platform:

```bash
flutterfire configure --project=posternung
```

## Testing

```bash
flutter test              # unit + widget tests
flutter test --coverage   # with lcov coverage report at coverage/lcov.info
```

`test/` mirrors `lib/` — usecases and repositories are unit-tested with `mocktail` (see `test/features/auth/`), widgets are tested via `ProviderScope` overrides so no test touches real Firebase. See **[CLAUDE.md § Testing](CLAUDE.md#testing)** for the conventions.

## Code Quality

```bash
dart format --output=none --set-exit-if-changed .   # formatting check
flutter analyze                                       # static analysis
```

Both run in CI on every push/PR — see [CI/CD](#cicd) below.

## Committing

This repo enforces [Conventional Commits](https://www.conventionalcommits.org/) via commitlint + husky. See **[CONTRIBUTING.md](CONTRIBUTING.md)** for setup and the exact format, or run `npm run commit` for a guided prompt.

## CI/CD

`.github/workflows/ci.yml` runs on every push to `main` and every pull request:

1. **format** — `dart format --set-exit-if-changed`
2. **analyze** — `flutter analyze`
3. **test** — `flutter test --coverage`, coverage report uploaded as a build artifact
4. **build** (fans out after format/analyze/test pass) — `flutter build apk --debug`, `flutter build ios --no-codesign`, `flutter build web`

All jobs are cached (Flutter SDK + pub packages) and cancel superseded runs on the same branch/PR automatically.
