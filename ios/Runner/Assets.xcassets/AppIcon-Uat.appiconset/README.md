Placeholder icon set for the UAT environment. `Contents.json` is copied from
`../AppIcon.appiconset/Contents.json` (same filenames/sizes), but the actual
PNG files are not included — export UAT-branded artwork at each listed size
and drop the PNGs in here. See `docs/environments-setup.md`.
