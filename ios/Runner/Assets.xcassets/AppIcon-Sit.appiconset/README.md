Placeholder icon set for the SIT environment. `Contents.json` is copied from
`../AppIcon.appiconset/Contents.json` (same filenames/sizes), but the actual
PNG files are not included — export SIT-branded artwork at each listed size
and drop the PNGs in here. See `docs/environments-setup.md`.
